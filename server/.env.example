# MongoDB Atlas 连接字符串
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/self-discipline-bet?retryWrites=true&w=majority

# JWT 密钥（使用 node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" 生成）
JWT_SECRET=your-secret-key-here-use-strong-random-string
JWT_REFRESH_SECRET=your-refresh-secret-here-use-another-strong-random-string

# Claude API Key
CLAUDE_API_KEY=sk-ant-xxxxx

# 服务器配置
PORT=5000
NODE_ENV=development
